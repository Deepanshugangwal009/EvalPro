{
  "name": "evalpro",
  "version": "1.0.0",
  "private": true,
  "description": "EvalPro - an online examination system built with Flask, MySQL, Jinja2 templates and vanilla JavaScript. This project has no Node.js runtime dependencies: Bootstrap is loaded from a CDN and the browser scripts in static/js are plain ES5. The file exists only to describe the project and to expose the common Python commands through npm.",
  "scripts": {
    "install:python": "pip install -r requirements.txt",
    "db:setup": "python init_db.py",
    "dev": "python app.py",
    "start": "gunicorn wsgi:app --bind 0.0.0.0:5000 --workers 2 --timeout 60"
  },
  "keywords": [
    "flask",
    "mysql",
    "online-examination",
    "jinja2"
  ],
  "author": "",
  "license": "ISC"
}
